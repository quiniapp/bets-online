// Shared layout component for login portals
export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return (
    <div>
      {/* Shared login layout will be implemented here */}
      {children}
    </div>
  );
}