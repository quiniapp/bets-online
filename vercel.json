{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm install && pnpm --filter helper build && pnpm --filter web build",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "outputDirectory": "web/.next",
  "framework": null
}
